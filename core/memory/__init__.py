# IRA Memory Package
# Components:
#   memory_engine.py   — MemoryStore (MEMORY.md + USER.md)
#   memory_tool.py     — Memory tool for Gemini integration
#   session_search.py  — FTS5 full-text session search
#   learning_graph.py  — Memory-skill connection graph
#   threat_patterns.py — Injection/exfiltration detection
#   provider_base.py   — Pluggable memory provider architecture
#   write_gate.py      — Memory write approval gate
#   memory_manager.py  — Legacy compatibility module (long_term.json) 