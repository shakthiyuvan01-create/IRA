# Headless IRA room-hub deps — minimal, for the 2013 MacBook Air (disk-full safe)
# Only what the voice loop + brahma_connect gateway need.
# NO PyQt6 / torch / whisper / opencv / mediapipe / playwright / pyautogui.
#
# TARGET PYTHON: 3.9.6 (system Python on the 2013 Mac — NO install needed)
# google-genai==1.47.0 is a `py3-none-any` wheel -> works on 3.9.
# Use `miniaudio` (not sounddevice): it vendors its own audio backend, so it
# works on a clean macOS WITHOUT system PortAudio / Homebrew.
# Pin miniaudio==1.71 — callback-based API (CaptureDevice/PlaybackDevice).
# Has a prebuilt cp39 macosx wheel; 1.58 does NOT ship a cp39 mac wheel on PyPI.

google-genai==1.47.0
miniaudio==1.71
fastapi>=0.110
uvicorn[standard]>=0.27
zeroconf>=0.131
cryptography==44.0.2   # last version with a prebuilt cp39 macosx wheel (50.x needs Xcode/OpenSSL source build)
python-dotenv>=1.0
websockets>=12.0
pydantic>=2.5
