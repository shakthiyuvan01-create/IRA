services:
  tool-runtime:
    build: ./dashboard/tool_runtime
    container_name: ira-tools
    volumes:
      - ./dashboard/custom_tools:/app/custom_tools
      - ira_tool_venv:/app/venv
    ports:
      - "8090:8090"
    restart: unless-stopped

volumes:
  ira_tool_venv:
